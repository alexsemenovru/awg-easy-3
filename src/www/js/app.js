'use strict';
(()=>{const api=window.awgApi,$=selector=>document.querySelector(selector),loginView=$('#login-view'),appView=$('#app-view'),logout=$('#logout'),clientsNode=$('#clients'),notice=$('#notice'),createDialog=$('#create-dialog'),profileDialog=$('#profile-dialog');let selectedClient;const showNotice=(message,error=false)=>{notice.textContent=message;notice.className=`notice ${error?'error':'success'}`;clearTimeout(showNotice.timer);showNotice.timer=setTimeout(()=>notice.classList.add('hidden'),5000)},showLogin=()=>{loginView.classList.remove('hidden');appView.classList.add('hidden');logout.classList.add('hidden')},showApp=()=>{loginView.classList.add('hidden');appView.classList.remove('hidden');logout.classList.remove('hidden')},guarded=async action=>{try{return await action()}catch(error){if(error.status===401)showLogin();showNotice(error.message,true);throw error}},update=async(client,changes,input)=>{input.disabled=true;try{await guarded(()=>api.updateClient(client.id,changes));await loadClients()}catch{input.checked=!input.checked}finally{input.disabled=false}},renderClient=client=>{const node=$('#client-template').content.firstElementChild.cloneNode(true);node.querySelector('.client-name').textContent=client.name;node.querySelector('.client-address').textContent=[client.address4,client.address6].filter(Boolean).join(' · ');const status=node.querySelector('.status');status.textContent=client.enabled?(client.networkGroup==='home'?'Home':'Guest'):'Отключён';status.className=`status ${client.enabled?client.networkGroup:'disabled'}`;const group=node.querySelector('.group-toggle');group.checked=client.networkGroup==='home';group.addEventListener('change',()=>update(client,{networkGroup:group.checked?'home':'guest'},group));const route=node.querySelector('.route-toggle');route.checked=client.routeMode==='ru_direct';route.addEventListener('change',()=>update(client,{routeMode:route.checked?'ru_direct':'vpn_all'},route));const enabled=node.querySelector('.enabled-toggle');enabled.checked=client.enabled;enabled.addEventListener('change',()=>update(client,{enabled:enabled.checked},enabled));node.querySelector('.show-profile').addEventListener('click',()=>openProfile(client));node.querySelector('.delete-client').addEventListener('click',async()=>{if(!confirm(`Удалить клиент «${client.name}»? Его профиль перестанет подключаться.`))return;await guarded(()=>api.deleteClient(client.id));showNotice('Клиент удалён');await loadClients()});return node},loadClients=async()=>{const clients=await guarded(()=>api.clients());clientsNode.replaceChildren(...clients.map(renderClient))},openProfile=client=>{selectedClient=client;$('#profile-title').textContent=client.name;$('#profile-qr').src=`${api.exportUrl(client.id,'qr-svg')}&t=${Date.now()}`;$('#download-config').href=api.exportUrl(client.id,'native-config');profileDialog.showModal()};$('#login-form').addEventListener('submit',async event=>{event.preventDefault();const password=$('#password');await guarded(()=>api.login(password.value));password.value='';showApp();await loadClients()});logout.addEventListener('click',async()=>{await api.logout();showLogin()});$('#show-create').addEventListener('click',()=>createDialog.showModal());document.querySelectorAll('.close-dialog').forEach(button=>button.addEventListener('click',()=>button.closest('dialog').close()));$('#create-form').addEventListener('submit',async event=>{event.preventDefault();const result=await guarded(()=>api.createClient({name:$('#client-name').value,networkGroup:$('#client-group').value,routeMode:$('#client-route').value}));createDialog.close();event.target.reset();await loadClients();openProfile(result.client)});$('#open-profile').addEventListener('click',async()=>{window.location.href=await guarded(()=>api.exportText(selectedClient.id,'vpn-link'))});$('#copy-profile').addEventListener('click',async()=>{await navigator.clipboard.writeText(await guarded(()=>api.exportText(selectedClient.id,'vpn-link')));showNotice('Ссылка скопирована')});$('#password-form').addEventListener('submit',async event=>{event.preventDefault();await guarded(()=>api.changePassword($('#current-password').value,$('#new-password').value));event.target.reset();showLogin();alert('Пароль изменён. Все старые сессии завершены — войдите снова.')});api.session().then(async({authenticated})=>{if(!authenticated)return showLogin();showApp();await loadClients()}).catch(error=>showNotice(error.message,true))})();
