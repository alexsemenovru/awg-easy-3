'use strict';
window.awgApi=(()=>{const request=async(url,options={})=>{const response=await fetch(url,{credentials:'same-origin',...options,headers:options.body?{'Content-Type':'application/json',...(options.headers||{})}:options.headers});if(!response.ok){let message=`Ошибка HTTP ${response.status}`;try{message=(await response.json()).error||message}catch{}const error=new Error(message);error.status=response.status;throw error}return response.headers.get('content-type')?.includes('application/json')?response.json():response.text()};const json=(method,body)=>({method,...(body===undefined?{}:{body:JSON.stringify(body)})});const clientPath=id=>`/api/v1/clients/${encodeURIComponent(id)}`;return Object.freeze({session:()=>request('/api/v1/session'),login:password=>request('/api/v1/session',json('POST',{password})),logout:()=>request('/api/v1/session',json('DELETE')),clients:()=>request('/api/v1/clients'),diagnostics:()=>request('/api/v1/diagnostics'),createClient:input=>request('/api/v1/clients',json('POST',input)),updateClient:(id,changes)=>request(clientPath(id),json('PATCH',changes)),deleteClient:id=>request(clientPath(id),json('DELETE')),exportUrl:(id,format)=>`${clientPath(id)}/export?format=${encodeURIComponent(format)}`,exportText:(id,format)=>request(`${clientPath(id)}/export?format=${encodeURIComponent(format)}`),changePassword:(currentPassword,newPassword)=>request('/api/v1/password',json('PUT',{currentPassword,newPassword}))})})();
